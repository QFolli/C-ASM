C++

#include <iostream>
int main() {
    int a = 7;
    int x = 5;
    int y1, y2, y;

    if (x > a) {
        y1 = x - a;
    }
    else {
        y1 = 5;
    }

    if (a > x) {
        y2 = a;
    }
    else {
        y2 = a * x;
    }

    y = y1 * y2;
}

Ассемблер

int main() {
    int a = 6;
    int x = 5;
    int y1, y2, y;

    __asm {
        mov eax, a
        mov ebx, x
        cmp ebx, eax
        jg x_greater_a
        jmp x_less_or_equal_a

        x_greater_a :
            mov ecx, a
            sub ebx, ecx  // x - a
            mov y1, ecx
            jmp define_y2

            x_less_or_equal_a :
            mov ecx, 5
            mov y1, ecx

            define_y2 :
            mov ecx, a
            cmp ecx, ebx
            jg a_greater_x
            jmp a_less_or_equal_x

            a_greater_x :
            mov y2, eax
            jmp solve

            a_less_or_equal_x :
            imul eax, ebx  // a * x
            mov y2, eax

            solve :
            mov eax, y1
            imul eax, y2
            mov y, eax
    }

Вопросы для зачета по работе:

1. Какая команда выполняет составление чисел?
add
2. Какой синтаксис команды предложения чисел?
приемник + источник = приемник

3. Какая команда выполняет предложение чисел с учётом флага переноса?
adc
4. Какой синтаксис команды предложения чисел с учётом флага переноса?
приемник + источник + CF = приемник

5. Какая команда осуществляет вычитание чисел?
sub
6. Какой синтаксис команды вычитания чисел?
приемник - источник = приемник

7. Какая команда осуществляет вычитание чисел с учётом флага переноса?
sbb
8. Какой синтаксис команды вычитания рассчитывается с учётом флага переноса?
приемник - источник - CF = приемник

9. Какая команда осуществляет умножение без знака?
mul
10. Какой синтаксис команды умножения без знака?
источник mul операнда8 ; результат в AX muloperand16 ; результат в DX:AX muloperand32 ; результат в EDX:EAX

11. Для чего используется флаг CF в городе умножения без знака?
Произошло ли переполнение при умножении.

12. Какая команда осуществляет умножение чисел со знакомыми?
imul
13. Для чего используется флаг OF в группе умножения чисел со знакомым?
Произошло ли переполнение при умножении.

14. Какой синтаксис команды умножения чисел вы знаете?
приемник * источник = приемник источник (8) = AX источник (16) = DX:AX источник (32) = EDX:EAX

15. Какую команду использовать для разделения чисел без знака?
div
16. Какая синтаксис команды разделения чисел без знака?
AX / источник (8) = AL:AH AX / источник (16) = AX:DX EAX / источник (32) = EAX:EDX

17. Какие команды использовались для расширения целого числа со знакомыми?
cbw - расширяет байтовое число, хранящееся в AL, до слов в AX, знаковым расширениям
cwd — расширение слова в AX до двойного слова в DX:AX, расширение знаков
cdq — расширяет двойное слово в DX:AX до четырех слов в EDX:EAX, обозначает расширение.
cqo — расширение четырех слов в EDX:EAX до восьми слов в RDX:RAX, расширение знаков.
18. Какой синтаксис имеет расширение целого числа со знакомым?
cbw, cwd, cdq, cqo
19. Какая команда использовалась для деления целого числа со знакомым?
idiv
20. Какой синтаксис имеет команду деления целого числа со знакомым?
AX / источник (8) = AL:AH AX / источник (16) = AX:DX EAX / источник (32) = EAX:EDX При этом операнды знаковые.

21. Какие ошибки могут возникнуть при использовании команды управления?
Деление на ноль и переполнение.
    
